import { 
    makeStyles, 
    Button, 
    ButtonGroup, 
    ThemeProvider, 
    createTheme, 
    Padding } from '@material-ui/core'
import { orange } from '@material-ui/core/colors'
import { SaveIcon, Home, Autorenew } from '@material-ui/icons/Save'

const useStyles = makeStyles({
    root: {
        flexGrow: 1
    },
    submitButton: {
        variant: 'contained',
        color: 'primary',
        type: 'submit',
        border: 0,
        borderRadius: 15,
        color: 'white',
    },
    button: {
        variant: 'contained',
        color: 'primary',
        border: 0,
        borderRadius: 15,
        color: 'white',
    },
    paper: {

    },
    textField: {

    }
})
